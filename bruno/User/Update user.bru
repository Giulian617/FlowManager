meta {
  name: Update user
  type: http
  seq: 12
}

put {
  url: {{url}}/users/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "firstName": "Test",
    "lastName": "Example",
    "role": "ADMIN"
  }
}
