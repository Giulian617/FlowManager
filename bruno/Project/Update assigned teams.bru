meta {
  name: Update assigned teams
  type: http
  seq: 5
}

put {
  url: {{url}}/projects/1/assign
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "assignedTeamsIds": [1, 2]
  }
}
