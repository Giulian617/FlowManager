app = "flowmanager-redis"
primary_region = "fra"

[build]

[mounts]
  source = "redis_data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = 256

[checks]
  [checks.alive]
    type = "tcp"
    port = 6379
    interval = "15s"
    timeout = "5s"
    grace_period = "30s"
