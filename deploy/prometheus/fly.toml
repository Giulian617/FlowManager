app = "flowmanager-prometheus"
primary_region = "fra"

[build]

[mounts]
  source = "prom_data"
  destination = "/prometheus"

[[vm]]
  size = "shared-cpu-1x"
  memory = 512

[checks]
  [checks.alive]
    type = "tcp"
    port = 9090
    interval = "15s"
    timeout = "5s"
    grace_period = "30s"
